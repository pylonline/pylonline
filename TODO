Agents will add bullet points for items that are still pending completion

- Switch from VirtualBox to WSL2 (not started; details: docs/runbooks/workspace/wsl2-migration.md)
- Install the 32GB RAM kit in the dev host when it arrives, then do the follow-ups:
  enable DOCP (~3200 MHz), revert Playwright local workers from 1 back to 2 in
  portal/playwright.portal.config.cjs, and give the VM back 8GB
  (docs/runbooks/workspace/dev-host-upgrades.md, TODO 1)
- Optional later: CPU upgrade to Ryzen 5000 so the RAM kit runs at its rated 3600
  (docs/runbooks/workspace/dev-host-upgrades.md, TODO 2)
- Back up org repos (main branches) to an encrypted USB (not started; drive chosen but
  procedure never run: docs/runbooks/workspace/backup-org-repos.md)
- Write the Pylon offline-first target architecture doc (pylon/docs/target-architecture.md
  is a "WIP" stub with no content)
